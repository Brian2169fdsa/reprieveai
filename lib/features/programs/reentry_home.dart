import 'package:flutter/material.dart';

class ReentryHomePage extends StatelessWidget {
  const ReentryHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Reentry Planning'),
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
