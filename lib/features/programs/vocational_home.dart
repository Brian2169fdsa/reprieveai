import 'package:flutter/material.dart';

class VocationalHomePage extends StatelessWidget {
  const VocationalHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vocational Programs'),
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
