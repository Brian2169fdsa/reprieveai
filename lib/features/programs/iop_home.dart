import 'package:flutter/material.dart';

class IopHomePage extends StatelessWidget {
  const IopHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Intensive Outpatient (IOP)'),
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
